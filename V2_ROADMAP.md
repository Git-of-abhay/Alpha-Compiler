# Alpha Compiler v2 roadmap

The v2 foundation turns division-by-zero from an interpreter traceback into an
explicit language runtime error and adds regression coverage. Next work is a
structured diagnostic model, source locations, and a command-line compiler.
